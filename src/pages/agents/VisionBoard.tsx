import React from "react";
import Layout from "../../components/layout";

const VisionBoard: React.FC = () => {
  return (
    <Layout>
      <div>
        <h1>Vision Board</h1>
        {/* Add vision board canvas and functionalities */}
      </div>
    </Layout>
  );
};

export default VisionBoard;
