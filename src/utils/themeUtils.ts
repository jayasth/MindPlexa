// src/utils/themeUtils.ts
export const setTheme = (theme: string) => {
  // Implement logic to set the selected theme
};

export const getTheme = () => {
  // Implement logic to get the current theme
};
